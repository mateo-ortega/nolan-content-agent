# Sesión 2026-04-23 — Sprint de reparación Nolan (Hermes)

**Plan de referencia:** `C:\Users\USUARIO\.claude\plans\act-a-como-mi-tech-robust-bee.md`
**Estado global:** EN PROGRESO — Bloques 0-3 DONE + Fixes calidad render DONE

---

## Contexto de la sesión

Mateo pidió originalmente migrar a otro framework tras una intervención fallida de Antigravity. La investigación confirmó:

1. **Esa migración fue abandonada en ronda 3 de discovery** (ver plan maestro). El stack real y vigente es Hermes Agent.
2. **La skill de carruseles no estaba corrompida** — el renderer externo `sapiens-carrusel/` se había vaciado localmente, pero el código de producción [`nolan-produce-carrusel/scripts/produce_carrusel.py`](skills/nolan-produce-carrusel/scripts/produce_carrusel.py) (509 líneas) está íntegro.
3. **El bloqueo real** es configuración del Gateway (personalidad `kawaii`) + falta de skills críticas (`nolan-research`, `nolan-produce-animacion`, `nolan-produce-voiceover`).

Decisión tomada por Mateo: **reparar Hermes quirúrgicamente, no migrar de plataforma**.

---

## Bitácora incremental (un bloque por sección)

### Bloque 0 — Snapshot + git init ✅ DONE (2026-04-23 17:27 COL)

**Acciones ejecutadas:**
- `git init -b main` en project root (antes NO era repo git)
- `git config core.autocrlf input` — evitar mutación de line endings
- `git add -A && git commit -m "snapshot: pre-repair state (2026-04-23)"`
- **Commit hash local:** `1cdc9d35b0753c703ba6f69e9f0ba81463d83151`
- VPS tar snapshot: `/home/mateo/sapiens-nolan-snapshot-20260423-222759.tar.gz` (740K, excluye staging/logs/sqlite)

**Hallazgos durante Bloque 0 que modifican plan siguiente:**

1. **Explore agent tuvo falsos negativos**. El `git add -A` reveló archivos que yo no había detectado:
   - [`skills/nolan-produce-guion/scripts/produce_guion.py`](skills/nolan-produce-guion/scripts/produce_guion.py) (85 líneas, existe) — implementación funcional pero con defectos: emoji 🎥 + hashtags genéricos en caption (viola SOUL), sin EthicsGate, sin prompt caching. El plan de "refundir en voiceover con --talking-head-only" sigue siendo correcto; puedo usar este archivo como referencia de código durante Bloque 6.
   - [`skills/nolan-producir/scripts/producir.py`](skills/nolan-producir/scripts/producir.py) (102 líneas, existe) — duplica lógica + emojis + fallback mudo. **Borrar sin conservar nada**.

2. **Schemas SQL ya existen completos** y más maduros de lo que propuse:
   - [`memory/schemas/trends.sql`](memory/schemas/trends.sql) (153 líneas) — tablas `signals_ig`, `signals_tt`, `signals_rss`, `signals_news`, `signals_trends`, `signals_clustered`, `candidate_sources`, `research_cycles`. Incluye virtual column de engagement, FK, índices. **Usar directamente en Bloque 5**.
   - [`memory/schemas/pieces.sql`](memory/schemas/pieces.sql) (145 líneas) — tabla `pieces` + `llm_usage` + vistas `v_monthly_cost`, `v_pieces_summary`, `v_approval_rate`, `color_history`. **Usar en Bloque 8 (callbacks).**

3. **VPS ya tiene `sapiens-carrusel` y `sapiens-animacion`** en `/srv/sapiens-nolan/skills/` (funcionales). El local está vacío (solo `.gitkeep`). Bloque 2 puede pullear del VPS como alternativa a `~/.claude/skills/`.

4. **Secretos del VPS**: `~/.hermes/.env` tiene 18957 bytes (grande, muchos secretos ya configurados). Verificar en Bloque 4 qué está presente vs pendiente.

**Git state:** repo local inicializado, 1 commit. NO se ha hecho `git remote add` ni push — consultar con Mateo si quiere GitHub/GitLab privado.

---

### Bloque 1 — Purga quirúrgica ✅ DONE

**Acciones ejecutadas:**
- Local: `rm -rf skills/nolan-producir skills/nolan-produce-guion`
- VPS `/srv/sapiens-nolan/skills/`: borrados ambos directorios
- VPS `~/.hermes/skills/`: borrados ambos directorios (Hermes carga skills aquí)
- **Commit local:** `b722a76ddf9e9b0a4adce074979536eb681b629d` — "bloque 1: purga nolan-producir y nolan-produce-guion (artefactos Antigravity)"

Nota: `~/.hermes/skills/` contiene 36 directorios bundled de Hermes base (apple, creative, data-science, devops, etc.) además de las `nolan-*` del agente. Solo tocamos las problemáticas.

---

### Bloque 2 — Restaurar renderer sapiens-carrusel + fix fixture ✅ DONE

**Acciones ejecutadas:**
- Copy canonical: `C:\Users\USUARIO\.claude\skills\sapiens-carrusel\*` → `skills/sapiens-carrusel/` del proyecto
- Verificación VPS: el renderer en `/srv/sapiens-nolan/skills/sapiens-carrusel/` ya era idéntico byte a byte (no tocado)
- **Bug pre-existente encontrado:** `content_min.yaml` sin campo `mode` hacía fallar `_validate_content_yaml` (línea 337 de `produce_carrusel.py`)
- **Fix aplicado:** agregar `mode: light|teal|deep|light|deep` a los 5 slides del fixture
- Deploy: `scp staging/fixtures/content_min.yaml` al VPS
- **Validación:** dry-run del pipeline carrusel pasa con `5 slides placeholder creados, costo=$0.0000, status=ready`

**Nota sobre estructura real del renderer:**
- NO hay `assets/templates/` subdir; los HTMLs (`portada.html`, `interior.html`, `cta.html`, `base.html`) están en `assets/`
- Los macros de gestos viven en `assets/gestos/_macros.html` (un solo archivo)
- `assets/tokens.css`, `assets/gestos/`, fuentes TTF, logos PNG, `sapiens_icon.png`
- `render.py` tiene solo 127 líneas, es minimalista

**Nota sobre .gitignore:** `skills/sapiens-carrusel/` está en `.gitignore` (excepto `.gitkeep`). El renderer vive en filesystem pero NO se versiona. Fuente canónica: `~/.claude/skills/sapiens-carrusel/`.

---

### Bloque 3 — Reparar Gateway Hermes 🟡 PARTIAL (bloqueado en sudo)

**Hallazgos del `~/.hermes/config.yaml` (364 líneas):**
- Línea 191: `display.personality: kawaii` ← causa raíz del bloqueo SESSION_REPORT
- Línea 50: `agent.tool_use_enforcement: auto` (conservado)
- Línea 230: `model.default: deepseek/deepseek-chat` ✅ ya correcto
- Línea 328: `timezone: ''` ← vacío, crítico para cron L-W-V
- Personalidad `engineer` NO EXISTE en Hermes. Opciones: catgirl, concise, creative, helpful, hype, kawaii, noir, philosopher, pirate, shakespeare, surfer, teacher, technical, uwu.
- `platform_toolsets.telegram: [hermes-telegram, skills, terminal, file]` ✅ Antigravity lo configuró correctamente

**Acciones ejecutadas (vía SSH, sin sudo):**
- Backup defensivo: `~/.hermes/config.yaml.bak-20260423-preblock3` (y SOUL/AGENTS similares)
- **Sincronización crítica descubierta:** `~/.hermes/SOUL.md` y `~/.hermes/AGENTS.md` estaban DESACTUALIZADOS respecto al source tree. La versión estricta "Silencio de Máquina / AUTONOMÍA ABSOLUTA" solo existía en `/srv/sapiens-nolan/`, no en `~/.hermes/`. **Sincronizados** (copy source tree → ~/.hermes/).
- **Edición config.yaml:**
  - `display.personality: kawaii` → `display.personality: concise`
  - `timezone: ''` → `timezone: America/Bogota`
  - `tool_use_enforcement: auto` ← conservado (más seguro; si Bloque 9 falla, cambiar a strict)
- Diff verificado contra backup: solo 2 líneas modificadas, limpio.

**Pendiente (requiere sudo interactivo de Mateo):**
- `sudo systemctl restart hermes-gateway` — NECESARIO para que el cambio surta efecto
- Verificación post-restart con `journalctl`
- Test Telegram (`/ping` desde el bot)

**Plan de acción para Mateo:** ver abajo en "Próximo paso manual".

---

### Fixes de calidad render ✅ DONE (2026-04-24)

**Contexto:** Primera producción real (pieza `2026-04-24-padres-tecnologia-educativa`) tuvo desbordamiento de texto en slides 05-07 + Drive subía archivos no-PNG.

**Fix 1 — render.py safe-zone detector** (`getBoundingClientRect`):
- Reemplazó el detector anterior (`scrollWidth > offsetWidth`) que tenía falsos negativos
- Ahora detecta correctamente todo elemento que supere el canvas 1080×1350 con tolerancia 2px
- Imprime `[OK]` o `[WARN]` por slide; stderr detalla violaciones por elemento

**Fix 2 — tokens.css CSS hardening**:
- `.content`: `min-width:0; max-width:100%; overflow:hidden`
- `.title`: `max-width:100%; overflow-wrap:break-word; word-break:normal; hyphens:auto`
- `.gesto-repeticion .rep`: `max-width:calc(100% - 120px); overflow-wrap:break-word`
- `.gesto-fragmentacion`: `overflow:hidden`; `.frag`: `max-width:calc(100% - 40px); overflow-wrap:break-word`
- `.gesto-bloque .host`, `.gesto-escala .hero`: `max-width:100%; overflow-wrap:break-word; hyphens:auto`

**Fix 3 — Reglas 11 y 12 en prompt** (`_CARRUSEL_SYSTEM_TEMPLATE`):
- Límites por gesto: `escala` palabra ≤8 chars; `fragmentacion` text ≤8 chars; `tachadura/inversion` ≤60/50 chars; `repeticion` ≤3 palabras; `body` ≤140 chars
- Regla 12 prohíbe compuestos largos sin reformular a sinónimo más corto

**Fix 4 — package.py rclone PNG-only**:
- `rclone copy ... --include "*.png"` para no subir YAML/HTML/MD a Drive

**Validación:** pieza fresca `2026-04-24-jovenes-habitos-icfes` → **7/7 slides [OK]**, costo $0.0635. Revisión visual completa: sin overflow en ningún gesto.

---

## Criterios de retoma si créditos se agotan

Si otro agente retoma esta sesión:
1. Leer este archivo COMPLETO.
2. Leer [plan final](C:\Users\USUARIO\.claude\plans\act-a-como-mi-tech-robust-bee.md) — hoja de ruta de 9 bloques.
3. Leer [HANDOFF.md](HANDOFF.md) — contexto del proyecto pre-sesión.
4. Leer memoria de Mateo (proyecto local de Claude Code) para contexto de sesiones previas.
5. Continuar desde el último bloque marcado DONE + arrancar el siguiente IN PROGRESS.
6. Respetar workflow de Mateo: paso a paso, comando exacto → esperar resultado. VPS por SSH key (sin password). PowerShell en Windows.

**Accesos verificados esta sesión:**
- SSH `<usuario_vps>@<VPS_IP>` — key-auth OK, VPS <vps-hostname> Ubuntu
- `/srv/sapiens-nolan/` — estructura completa presente
- venv: `/srv/nolan-venv/` con Python 3.12.3
